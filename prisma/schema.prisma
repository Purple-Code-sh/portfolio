generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url=env("POSTGRES_URL")
}

model Landing {
  id           Int      @id @default(autoincrement())
  name         String   @unique
  imagePreview String
  htmlSrc      String
  technologies String[]
  description  String
}